# ─── Machine-specific MCP config ──────────────────────────────────────────────
# Only produced by the setup.js fallback; the plugin route needs no such file.
# Contains absolute paths, and historically contained credentials.
#
# NOTE: the repo-root mcp.json is the PLUGIN's server definition. It is
# intentionally committed — it holds only placeholders, never credentials.
.cursor/mcp.json
.env
.env.local

# ─── Runtime credential and token storage ─────────────────────────────────────
# Written by the MCP server at login time. Contains bearer tokens and client IDs.
# Covered at BOTH the repo root and mcp-server/ (server may write to either
# location depending on its working directory when started).
.sdd-summary/
mcp-server/.sdd-summary/

# ─── Node build output and dependencies ───────────────────────────────────────
# dist/ is the local tsc output, used for type-checking and debugging only.
# mcp-server/bundle/ is deliberately NOT ignored: Cursor installs a plugin by
# cloning the repo without running a build, so the bundled server must be
# committed or the plugin has nothing to run.
mcp-server/dist/
mcp-server/node_modules/

# ─── One-off scripts (not part of the package) ────────────────────────────────
mcp-server/scripts/

# ─── Summary config lifecycle data ───────────────────────────────────────────
# Contains customer transcripts (PII), eval runs, test data, and credentials.
# Nothing under this directory should ever be committed.
.summaryconfig-lifecycle/

# ─── Local archive (contains credentials, test data, build artifacts) ─────────
archived/

# ─── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store
